{/* Introduction.mdx */}
import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Introduction" />

# Gather Ground — Component Library

Marketing website for a heritage-breed family farm in rural Iowa.

This Storybook documents every UI component used on the Gather Ground website.
Each story is linked to its Figma source, includes a live accessibility audit,
and has interaction tests you can run in-browser.

---

## Stack

<table>
  <thead>
    <tr>
      <th>Layer</th>
      <th>Tool</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Framework</td>
      <td>Astro (static) + React islands</td>
    </tr>
    <tr>
      <td>Styling</td>
      <td>Tailwind CSS v4 + design tokens</td>
    </tr>
    <tr>
      <td>Primitives</td>
      <td>shadcn/ui</td>
    </tr>
    <tr>
      <td>CMS</td>
      <td>Storyblok</td>
    </tr>
    <tr>
      <td>Testing</td>
      <td>Storybook interactions + Playwright</td>
    </tr>
  </tbody>
</table>

---

## Panels

Every story exposes three panels at the bottom of the canvas:

- **Controls** — adjust props live without touching code
- **Accessibility** — WCAG audit for the rendered story; fix any violations before merging
- **Design** — side-by-side Figma frame linked to the exact component spec

---

## Component categories

### Components

Built in Astro (server-rendered, zero client JS unless noted) using shadcn/ui primitives and Gather Ground tokens.

- `Components/Button` — primary, outline, ghost, and link variants; renders as `<button>` or `<a>` depending on `href`
- `Components/Footer` — full-width footer with logo, nav link groups, social icons, newsletter form, and legal bar
- `Components/NewsletterForm` — email subscribe input with success state (React island)
- `Components/SectionDivider` — 1 px horizontal rule between page sections; `sm`, `md`, and `lg` spacing variants

---

## Design tokens quick reference

All tokens live in `src/styles/global.css` and are exposed as Tailwind utility classes. Never hardcode hex values.

### Colour

<table>
  <thead>
    <tr>
      <th>Purpose</th>
      <th>Token</th>
      <th>Class</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Page background</td>
      <td>
        <code>off-white</code>
      </td>
      <td>
        <code>bg-off-white</code>
      </td>
    </tr>
    <tr>
      <td>Primary text</td>
      <td>
        <code>brand-900</code>
      </td>
      <td>
        <code>text-brand-900</code>
      </td>
    </tr>
    <tr>
      <td>Section headings</td>
      <td>
        <code>gray-900</code>
      </td>
      <td>
        <code>text-gray-900</code>
      </td>
    </tr>
    <tr>
      <td>Muted / captions</td>
      <td>
        <code>gray-600</code>
      </td>
      <td>
        <code>text-gray-600</code>
      </td>
    </tr>
    <tr>
      <td>Primary button</td>
      <td>
        <code>brand-700</code>
      </td>
      <td>
        <code>bg-brand-700</code>
      </td>
    </tr>
    <tr>
      <td>Footer bg</td>
      <td>
        <code>gray-950</code>
      </td>
      <td>
        <code>bg-gray-950</code>
      </td>
    </tr>
    <tr>
      <td>Card background</td>
      <td>
        <code>secondary-50</code>
      </td>
      <td>
        <code>bg-secondary-50</code>
      </td>
    </tr>
  </tbody>
</table>

### Typography

Font: **DM Sans** (variable). All sizes use standard Tailwind text utilities.

<table>
  <thead>
    <tr>
      <th>Scale</th>
      <th>Class</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Body default</td>
      <td>
        <code>text-base font-normal</code>
      </td>
    </tr>
    <tr>
      <td>Body semibold</td>
      <td>
        <code>text-base font-semibold</code>
      </td>
    </tr>
    <tr>
      <td>Section heading</td>
      <td>
        <code>text-display-md font-semibold tracking-display-md</code>
      </td>
    </tr>
    <tr>
      <td>Hero heading</td>
      <td>
        <code>text-display-xl font-medium tracking-display-xl</code>
      </td>
    </tr>
  </tbody>
</table>

See [`TOKENS.md`](../../TOKENS.md) for the full token reference.

---

## Conventions

- Write `.astro` components for static output; use `.tsx` React islands only when client interactivity is required
- Pass `client:visible` (not `client:load`) unless immediate mount interactivity is needed
- Fetch data in Astro page frontmatter — never inside a component
- No inline styles; no hardcoded colours or spacing values
- Define the TypeScript interface in `src/types/` **before** writing any component markup
- Run `npm run format` before every commit; CI will reject unformatted files

---

## Running locally

```sh
# Component library
npm run storybook

# Full site dev server
npm run dev

# Type safety
npm run typecheck

# E2E tests
npx playwright test --ui
```
