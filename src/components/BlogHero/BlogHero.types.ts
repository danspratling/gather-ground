export interface BlogHeroProps {
  eyebrow: string;
  heading: string;
  subCopy: string;
  /** Optional — hides the email form when omitted */
  privacyPolicyHref?: string;
  class?: string;
}

export default null;
