export interface DetailsAlternatingProps {
  icon: string;
  heading: string;
  body: string;
  checklistItems: string[];
  image: { src: string; alt: string };
  imagePosition?: 'left' | 'right';
  dark?: boolean;
}

export default null;
